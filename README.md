# Kernel-Debug-Kit
Performing Two-Machine Debugging The KDK supports the debugging of kernel-level code, such as kernel extensions, from a second Mac.  The target device is the Mac that runs the code you want to debug. The host device is the Mac that runs the debugger.
